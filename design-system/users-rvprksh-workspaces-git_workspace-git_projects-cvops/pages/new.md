# New resume (`/new`)

> Overrides `../MASTER.md`. Only deviations and page-specific rules are listed; everything else follows the Master.

- One decision, two equal cards: **Upload your existing resume** (tagged "Fastest") and **Build from scratch**. Each states what it does and who it is for; the whole card is one link target.
- Below the cards, `Steps` with step 1 highlighted answers "what happens next". No other actions on the page.
- The two cards live only here; the empty dashboard links to this page instead of repeating them.
- Decorative `ParticleMorph` (resume page <-> globe, looping) sits beside the title from `md` up; hidden on phones. It is the only place the morph is a hero; it belongs where a first decision is made, not in the persistent navigation.
