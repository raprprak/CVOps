# Build from scratch (`/builder`)

> Overrides `../MASTER.md`. Only deviations and page-specific rules are listed; everything else follows the Master.

- Breadcrumb + `Steps` (step 1) above a two-pane layout: form left, opaque white ATS preview right (never glass).
- The form starts blank. Contact details are shown read-only from the career data. Dates are month pickers.
- The Build card is first in the left pane; success offers Open PDF, "Next: tailor it to a job" and the dashboard.
